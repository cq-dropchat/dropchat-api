# Env
.env*

# Editors
.vscode
.zed

# Claude Code per-user overrides (project-shared config lives in .claude/settings.json)
.claude/settings.local.json

# Supabase
supabase/.branches
supabase/.temp

# Node
node_modules
package.json
package-lock.json

mise.toml

.mcp.json

# Deno test coverage
supabase/functions/coverage/

# Salida de la herramienta de grafo de conocimiento: artefactos generados,
# uno de ellos un JSON de una sola línea que pone `deno fmt --check` en rojo.
graphify-out/
